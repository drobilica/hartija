{% extends "base.html.j2" %}
{% block content %}

  {% for key,value in entries.iterrows() %}

  <ul>
      <li>
           <div class="article_content">
             <span>{{ value.title|e }}</span>
             <div class="summary">
               {{ value.summary|e }}</br>
               <a href="{{ value.link }}">Read More</a>
             </div>
           </div>
       </li>
  </ul>
  {% endfor %}
{% endblock %}
